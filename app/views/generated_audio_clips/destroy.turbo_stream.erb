<%# FIRST: Always remove the audio clip itself. %>
<%= turbo_stream.remove @generated_audio_clip %>

<% if @remove_date_group %>
  <%# ALSO remove the date group if there are no more clips for that date. %>
  <%= turbo_stream.remove "history_date_group_#{@date_to_check.to_s}" %>
<% end %>