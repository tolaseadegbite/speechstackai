<%# --- Mobile Player (Hidden on LG and up) (Original structure with functional hooks) --- %>
<div class="hide@lg p-4">

  <%# Section 1: Progress Bar (with seek functionality) %>
  <button type="button" class="btn btn--plain i-full" data-action="player-visibility#expand audio-player#seek" data-audio-player-target="progressContainers">
    <div class="relative i-full bg-shade rounded-full" style="block-size: 2px;">
      <div class="absolute b-full bg-black rounded-full" style="inline-size: 0%;" data-audio-player-target="progressBars"></div>
    </div>
  </button>

  <%# Section 2: Collapsible Content (structure fully restored) %>
  <div class="relative flex items-center justify-between gap-4 pbs-2" data-player-visibility-target="collapsible">
    <!-- Left Section: Clip Info -->
    <div class="flex flex-col min-i-0">
      <p class="font-medium text-primary overflow-ellipsis" data-audio-player-target="clipTitles">This is my generated clip title.</p>
      <div class="flex items-center gap-half text-sm text-subtle mbs-1">
        <div class="rounded-full" style="inline-size: 1.25rem; block-size: 1.25rem; background: linear-gradient(180deg, var(--gradient-start, #FDBFDE) 0%, var(--gradient-end, #E66FB2) 100%);"
          data-audio-player-target="gradientSwatch"></div>
          
        <span class="font-medium" data-audio-player-target="clipAuthors">Tolase</span>
        
        <span>·</span>

        <svg class="shrink-0" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"></circle><polyline points="12 6 12 12 16 14"></polyline></svg>

        <span data-audio-player-target="clipDates"></span>
      </div>
    </div>

    <!-- Right Section: Actions (Actions added) -->
    <div class="flex items-center gap-2 shrink-0">
      <%# The download button is now a functional link, styled to look the same %>
      <a href="#" class="btn btn--icon border-0" data-audio-player-target="downloadLinks"><span class="icon icon--download" aria-hidden="true"></span></a>
      <button class="bg-black text-reversed rounded-full p-2 border-0" data-action="audio-player#togglePlay" data-audio-player-target="playPauseIcons"><svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="currentColor"><path d="M5 3l14 9-14 9V3z"/></svg></button>
      <button type="button" class="btn btn--icon border-0" data-action="click->player-visibility#minimize">
        <span class="icon icon--chevron-down" aria-hidden="true"></span>
      </button>
    </div>
  </div>
</div>